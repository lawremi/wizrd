IOBinding <- new_class("IOBinding",
                       properties = list(
                           input = SerialFormat,
                           output = SerialFormat
                       ))
