FormatBinding <- new_class("FormatBinding",
                           properties = list(
                               input = SerialFormat,
                               output = SerialFormat
                           ))
